{
  "name": "designs-of-dreams",
  "version": "1.0.0",
  "description": "Designs of Dreams Monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "dodshop",
    "Dashbord"
  ],
  "scripts": {
    "build:dodshop": "npm run db:generate --workspace=@dod/database && npm run build --workspace=dodshop",
    "build:dashboard": "npm run db:generate --workspace=@dod/database && npm run build --workspace=atelier-admin",
    "db:generate": "npm run db:generate --workspace=@dod/database"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
